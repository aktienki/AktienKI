<x-app-layout>
    <main class="ak-body min-h-[calc(100dvh-73px)] py-5 sm:py-8">
        <div class="ak-container">
            <header class="mb-5 flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between">
                <div><p class="text-[10px] font-black uppercase tracking-[.22em] text-cyan-500">Serving · aktuelle Releases</p><h1 class="mt-1 text-2xl font-black text-[var(--ak-text)] sm:text-3xl">Reguläre Backtest-Trades</h1><p class="mt-2 max-w-4xl text-sm leading-6 text-[var(--ak-muted)]">Jede Position bleibt bis zum vorgesehenen Horizont geöffnet. Angezeigt werden nur Trades des derzeit aktiven Releases einer Aktie.</p></div>
                <div class="grid grid-cols-2 gap-2 sm:grid-cols-5">
                    @foreach([
                        ['Trades',(int)($summary->trades??0),'text-cyan-400'],
                        ['Aktien',(int)($summary->stocks??0),'text-violet-400'],
                        ['Hitrate',is_numeric($summary->hit_rate??null)?number_format($summary->hit_rate,1,',','.').' %':'—','text-emerald-400'],
                        ['Ø / Trade',is_numeric($summary->average_return??null)?sprintf('%+.2f %%',$summary->average_return):'—',(float)($summary->average_return??0)>=0?'text-emerald-400':'text-rose-400'],
                        ['Profitfaktor',is_numeric($summary->profit_factor??null)?number_format($summary->profit_factor,2,',','.'):'—','text-amber-400'],
                    ] as [$label,$value,$tone])<div class="rounded-xl border border-[var(--ak-border)] bg-[var(--ak-card)] px-3 py-2 text-right"><b class="block text-xl font-black tabular-nums {{ $tone }}">{{ $value }}</b><small class="block text-[7px] font-black uppercase tracking-wider text-[var(--ak-muted)]">{{ $label }}</small></div>@endforeach
                </div>
            </header>

            <section class="ak-card mb-4 p-3 sm:p-4"><form method="GET" class="flex flex-wrap gap-2"><input name="q" value="{{ request('q') }}" placeholder="Name oder Symbol" class="ak-input min-w-64" /><select name="horizon" class="ak-input"><option value="">Alle Horizonte</option>@foreach([10,20,40] as $value)<option value="{{ $value }}" @selected((int)request('horizon')===$value)>{{ $value }}T</option>@endforeach</select><select name="variant" class="ak-input"><option value="">Standard und TCN</option><option value="standard" @selected(request('variant')==='standard')>Standard</option><option value="pure_tcn" @selected(request('variant')==='pure_tcn')>TCN</option></select><button class="ak-button-primary">Anwenden</button><a class="ak-button-secondary" href="{{ route('predictions.trade-performance.backtest') }}">Zurücksetzen</a></form></section>

            <section class="ak-card overflow-hidden border-cyan-400/25"><div class="overflow-x-auto"><table class="w-full min-w-[1200px] border-collapse text-left"><thead class="bg-cyan-400/[.055] text-[9px] font-black uppercase tracking-[.12em] text-[var(--ak-muted)]"><tr><th class="px-4 py-3">Aktie</th><th>Modell</th><th>Horizont</th><th>Einstieg</th><th>Ausstieg</th><th>Haltedauer</th><th>Netto-Rendite</th><th>TCN Entry / Exit</th><th>Exit-Grund</th><th class="pr-4">Berechnet</th></tr></thead><tbody class="divide-y divide-[var(--ak-border)]">
                @forelse($trades as $trade)
                    @php $isTcn = str_starts_with(strtolower((string)$trade->strategy),'pure-tcn'); $return=(float)$trade->net_return*100; @endphp
                    <tr class="text-xs text-[var(--ak-text)] hover:bg-cyan-400/[.025]"><td class="px-4 py-3"><a href="{{ route('stocks.show',['symbol'=>$trade->symbol]) }}" class="block max-w-60 truncate font-black hover:text-cyan-400">{{ $trade->name ?: $trade->symbol }}</a><small class="text-[9px] text-[var(--ak-muted)]">{{ $trade->country_code }} · {{ $trade->symbol }}</small></td><td><span class="rounded-md border px-2 py-1 text-[9px] font-black uppercase {{ $isTcn?'border-violet-400/40 text-violet-400':'border-cyan-400/40 text-cyan-400' }}">{{ $isTcn?'TCN':'Standard' }}</span><small class="mt-1 block max-w-52 truncate text-[8px] text-[var(--ak-muted)]">{{ $trade->strategy }}</small></td><td><b class="text-base text-cyan-400">{{ $trade->horizon }}T</b></td><td><b>{{ \Illuminate\Support\Carbon::parse($trade->entry_date)->format('d.m.Y') }}</b><small class="block text-[9px] text-[var(--ak-muted)]">{{ number_format((float)$trade->entry_close_eur,2,',','.') }} €</small></td><td><b>{{ \Illuminate\Support\Carbon::parse($trade->exit_date)->format('d.m.Y') }}</b><small class="block text-[9px] text-[var(--ak-muted)]">{{ number_format((float)$trade->exit_close_eur,2,',','.') }} €</small></td><td><b>{{ $trade->holding_days }}</b> Handelstage</td><td><b class="text-base {{ $return>=0?'text-emerald-400':'text-rose-400' }}">{{ sprintf('%+.2f %%',$return) }}</b></td><td><b>{{ is_numeric($trade->entry_tcn_score)?number_format((float)$trade->entry_tcn_score*100,2,',','.').' %':'—' }}</b><small class="block text-[8px] text-[var(--ak-muted)]">Exit {{ is_numeric($trade->exit_tcn_score)?number_format((float)$trade->exit_tcn_score*100,2,',','.').' %':'—' }}</small></td><td><span class="text-[9px] font-bold text-[var(--ak-muted)]">{{ str_replace('_',' ',$trade->exit_reason) }}</span></td><td class="pr-4"><small class="text-[9px] text-[var(--ak-muted)]">{{ \Illuminate\Support\Carbon::parse($trade->calculation_date)->format('d.m.Y') }}</small></td></tr>
                @empty<tr><td colspan="10" class="px-6 py-14 text-center text-sm text-[var(--ak-muted)]">Keine Trades mit diesen Filtern vorhanden.</td></tr>@endforelse
            </tbody></table></div></section>
            <div class="mt-5">{{ $trades->links() }}</div><p class="mt-3 text-[9px] text-[var(--ak-muted)]">Quelle: serving_strategy_runs + serving_strategy_trades · nur aktive Release-IDs.</p>
        </div>
    </main>
</x-app-layout>
