<x-app-layout>
    <main class="ak-body min-h-[calc(100dvh-73px)] py-5 sm:py-8"><div class="ak-container">
        <header class="mb-5"><p class="text-[10px] font-black uppercase tracking-[.22em] text-cyan-500">Serving · Prediction-Kontext</p><h1 class="mt-1 text-2xl font-black text-[var(--ak-text)] sm:text-3xl">ChartView-Signale</h1><p class="mt-2 max-w-4xl text-sm leading-6 text-[var(--ak-muted)]">Die Tabelle liest Chart-Signale ausschließlich aus dem kompakten Kontext freigegebener Serving-Predictions.</p></header>
        <section class="ak-card overflow-hidden border-cyan-400/25"><div class="overflow-x-auto"><table class="w-full min-w-[820px] border-collapse text-left"><thead class="bg-cyan-400/[.055] text-[9px] font-black uppercase tracking-[.12em] text-[var(--ak-muted)]"><tr><th class="px-4 py-3">Aktie</th><th>Ereignis</th><th>Horizont</th><th>Modellsignal</th><th>Wahrscheinlichkeit</th><th class="pr-4">Stand</th></tr></thead><tbody class="divide-y divide-[var(--ak-border)]">@forelse($signals as $row)<tr class="text-xs text-[var(--ak-text)]"><td class="px-4 py-3"><a class="font-black hover:text-cyan-400" href="{{ route('stocks.show',['symbol'=>$row->symbol]) }}">{{ $row->name ?: $row->symbol }}</a><small class="block text-[9px] text-[var(--ak-muted)]">{{ $row->symbol }}</small></td><td>{{ $row->event }}</td><td>{{ $row->horizon }}T</td><td><b>{{ $row->signal }}</b></td><td>{{ is_numeric($row->probability)?number_format($row->probability,1,',','.').' %':'—' }}</td><td class="pr-4">{{ \Illuminate\Support\Carbon::parse($row->as_of)->format('d.m.Y H:i') }}</td></tr>@empty<tr><td colspan="6" class="px-6 py-14 text-center"><b class="block text-sm text-[var(--ak-text)]">Noch keine Serving-Chart-Signale</b><span class="mt-2 block text-xs text-[var(--ak-muted)]">Es gibt derzeit keine freigegebene Prediction mit Chart-Signal-Kontext. Legacy-ChartView-Ereignisse werden absichtlich nicht angezeigt.</span></td></tr>@endforelse</tbody></table></div></section><div class="mt-5">{{ $signals->links() }}</div><p class="mt-3 text-[9px] text-[var(--ak-muted)]">Quelle: serving_predictions.compact_context.</p>
    </div></main>
</x-app-layout>
