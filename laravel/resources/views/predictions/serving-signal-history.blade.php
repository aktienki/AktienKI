<x-app-layout>
    <main class="ak-body min-h-[calc(100dvh-73px)] py-5 sm:py-8"><div class="ak-container">
        <header class="mb-5 flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><p class="text-[10px] font-black uppercase tracking-[.22em] text-cyan-500">Serving · freigegebene Predictions</p><h1 class="mt-1 text-2xl font-black text-[var(--ak-text)] sm:text-3xl">Signalwechsel-Historie</h1><p class="mt-2 max-w-4xl text-sm leading-6 text-[var(--ak-muted)]">Ein Wechsel wird nur gespeichert, wenn ein freigegebener aktiver Horizont tatsächlich eine neue Prediction erzeugt. Alte Signale werden nicht ergänzt.</p></div><div class="grid grid-cols-5 gap-2">@foreach([['Gesamt',$summary->total,'text-cyan-400'],['BUY',$summary->buy,'text-emerald-400'],['WATCH',$summary->watch,'text-lime-400'],['HOLD',$summary->hold,'text-amber-400'],['SELL',$summary->sell,'text-rose-400']] as [$label,$value,$tone])<div class="min-w-20 rounded-xl border border-[var(--ak-border)] bg-[var(--ak-card)] px-3 py-2 text-right"><b class="block text-xl font-black {{ $tone }}">{{ $value }}</b><small class="text-[7px] font-black uppercase text-[var(--ak-muted)]">{{ $label }}</small></div>@endforeach</div></header>
        <section class="ak-card mb-4 p-3"><form method="GET" class="flex flex-wrap gap-2"><input name="q" value="{{ request('q') }}" placeholder="Name oder Symbol" class="ak-input min-w-64" /><select name="signal" class="ak-input"><option value="">Alle neuen Signale</option>@foreach(['BUY','WATCH','WAIT','HOLD','SELL'] as $signal)<option value="{{ $signal }}" @selected(request('signal')===$signal)>{{ $signal }}</option>@endforeach</select><button class="ak-button-primary">Anwenden</button><a class="ak-button-secondary" href="{{ route('predictions.signal-history') }}">Zurücksetzen</a></form></section>
        <section class="ak-card overflow-hidden border-cyan-400/25"><div class="overflow-x-auto"><table class="w-full min-w-[920px] border-collapse text-left"><thead class="bg-cyan-400/[.055] text-[9px] font-black uppercase tracking-[.12em] text-[var(--ak-muted)]"><tr><th class="px-4 py-3">Aktie</th><th>Wechsel</th><th>Zeitpunkt</th><th>Kurs</th><th>Score</th><th>Risiko</th><th class="pr-4">Release</th></tr></thead><tbody class="divide-y divide-[var(--ak-border)]">@forelse($transitions as $row)<tr class="text-xs text-[var(--ak-text)]"><td class="px-4 py-3"><a href="{{ route('stocks.show',['symbol'=>$row->symbol]) }}" class="font-black hover:text-cyan-400">{{ $row->name ?: $row->symbol }}</a><small class="block text-[9px] text-[var(--ak-muted)]">{{ $row->country_code }} · {{ $row->symbol }}</small></td><td><b class="text-[var(--ak-muted)]">{{ $row->from_signal }}</b><span class="mx-2">→</span><b class="{{ $row->to_signal==='BUY'?'text-emerald-400':($row->to_signal==='SELL'?'text-rose-400':'text-amber-400') }}">{{ $row->to_signal }}</b></td><td>{{ \Illuminate\Support\Carbon::parse($row->changed_at)->format('d.m.Y H:i') }}</td><td>{{ is_numeric($row->price_at_change)?number_format($row->price_at_change,2,',','.').' '.$row->currency:'—' }}</td><td>{{ $row->score_at_change ?: '—' }}</td><td>{{ $row->risk_at_change ?? '—' }}</td><td class="pr-4"><small class="font-mono text-[8px] text-[var(--ak-muted)]">{{ Str::limit($row->release_id,13) }}</small></td></tr>@empty<tr><td colspan="7" class="px-6 py-14 text-center"><b class="block text-sm text-[var(--ak-text)]">Noch keine neuen Signalwechsel</b><span class="mt-2 block text-xs text-[var(--ak-muted)]">Aktuell existieren noch keine freigegebenen Serving-Predictions. Das ist der korrekte Zustand und kein Rückgriff auf alte Daten.</span></td></tr>@endforelse</tbody></table></div></section><div class="mt-5">{{ $transitions->links() }}</div><p class="mt-3 text-[9px] text-[var(--ak-muted)]">Quelle: serving_signal_transitions.</p>
    </div></main>
</x-app-layout>
