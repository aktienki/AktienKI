document.addEventListener('DOMContentLoaded',()=>{const targets=document.querySelectorAll('.welcome-flow-card,.welcome-feature-card,.welcome-price-card,.welcome-dashboard');if(window.matchMedia('(prefers-reduced-motion: reduce)').matches||!('IntersectionObserver'in window)){targets.forEach(el=>el.classList.add('welcome-is-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('welcome-is-visible');observer.unobserve(entry.target)}})},{threshold:.12});targets.forEach((el,index)=>{el.style.setProperty('--welcome-reveal-delay',`${Math.min(index%6,5)*80}ms`);observer.observe(el)})})